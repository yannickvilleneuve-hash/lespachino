# Plan 2 — Listings Supabase + Admin `/inventaire` + Photos

> **For agentic workers:** REQUIRED SUB-SKILL: `superpowers:subagent-driven-development` (recommandé) ou `superpowers:executing-plans`. Étapes en `- [ ]` pour tracking.

**Goal:** Admin/vendeur connecté peut voir les 66 véhicules actifs de SERTI, éditer prix/description/canaux de publication/statut publié, et uploader-réordonner jusqu'à 15 photos par VIN. Coûtant SERTI (`WGICST`) affiché read-only en edit pour aider à fixer le prix. Pas de feeds publics (Plan 3).

**Success criteria (tous verts avant Plan 3)**:
1. Login → `/inventaire` → liste des véhicules `WGISTA='A'` enrichie avec prix/publié (join `listing`)
2. Clic sur une ligne → `/inventaire/[vin]` affiche:
   - WGI data (make/model/year/km/color) readonly
   - **`WGICST` readonly** (coûtant interne)
   - Form: prix CAD, description FR, toggle publié, checkboxes canaux (native/fb/lespac/kijiji)
   - Photo manager: upload multi, drag-to-reorder, marqueur hero (1 photo = auto-hero), suppression
3. Sauvegarde → retour liste → badge "Publié" visible sur les lignes publiées
4. Règle publication: **impossible de toggle publié à `true` sans** (`price_cad > 0` + `description_fr` non vide + ≥1 photo dont 1 hero)
5. RLS actif: tout utilisateur authentifié a SELECT+INSERT+UPDATE+DELETE; anon a 0 accès
6. `pnpm test`, `pnpm typecheck`, `pnpm lint` verts; CI verte

**Architecture:**
- Supabase schema `public`: tables `listing`, `vehicle_photo`
- Supabase Storage bucket `vehicle-photos` (privé, signed URLs 1h pour affichage)
- Next.js Server Components + Server Actions (pas de REST custom)
- Client SERTI existant (Plan 1) pour lire WGI
- Pas de rôles différenciés Plan 2 (simple `authenticated`); hiérarchie admin/vendeur = Plan 4

**Tech:**
- `@supabase/ssr` déjà installé
- Ajouts: `react-hook-form`, `zod`, `@dnd-kit/core` + `@dnd-kit/sortable` (drag-reorder photos)

**Hors scope:**
- Feeds XML/CSV publication → Plan 3
- Catalogue public `/` style Lespac → Plan 3
- Leads (bouton appeler + form contact) → Plan 4
- Admin séparation de rôles (admin vs vendeur) → Plan 4

---

## File Structure

**À créer:**

- `supabase/migrations/20260422120000_listings_photos.sql` — schema + RLS + bucket (DDL via `apply_migration`)
- `lib/supabase/types.ts` — types générés via MCP `generate_typescript_types`
- `lib/listings/schema.ts` — Zod schemas (listing form, photo metadata)
- `lib/listings/queries.ts` — server-side fetchers (list + byVin, merge WGI+listing+photos)
- `lib/listings/actions.ts` — Server Actions (`upsertListing`, `togglePublished`, `uploadPhoto`, `deletePhoto`, `reorderPhotos`, `setHero`)
- `lib/listings/publication.ts` — validateur "prêt à publier"
- `app/inventaire/page.tsx` — liste
- `app/inventaire/[vin]/page.tsx` — edit
- `app/inventaire/[vin]/photo-manager.tsx` — Client Component (dnd + upload)
- `app/inventaire/[vin]/listing-form.tsx` — Client Component (react-hook-form)
- `tests/unit/publication.test.ts` — règle publication
- `tests/unit/listings-queries.test.ts` — merge WGI+listing mock

**À modifier:**

- `app/dashboard/page.tsx` — ajouter lien `/inventaire`
- `middleware.ts` — étendre `needsAuth` à `/inventaire`

---

## Task 0: Schéma Supabase + RLS + Storage bucket

**Goal:** Schema en place via migration, RLS testée, bucket privé prêt.

**Verify:** `listing` + `vehicle_photo` existent dans `public`, RLS enabled, bucket `vehicle-photos` privé, anon select → 0 row, authenticated select → OK.

- [ ] **Step 1: Migration DDL**

Contenu de `supabase/migrations/20260422120000_listings_photos.sql` (appliqué via `mcp__plugin_supabase_supabase__apply_migration` — le fichier sert de trace dans git).

```sql
-- Catalogue prix + métadonnées vente, une ligne par véhicule SERTI
CREATE TABLE public.listing (
  vin            text PRIMARY KEY,
  price_cad      numeric(10,2) NOT NULL DEFAULT 0,
  description_fr text NOT NULL DEFAULT '',
  is_published   boolean NOT NULL DEFAULT false,
  channels       text[] NOT NULL DEFAULT ARRAY['native','fb','lespac','kijiji'],
  updated_by     uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  created_at     timestamptz NOT NULL DEFAULT now(),
  updated_at     timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX listing_published_idx ON public.listing(is_published) WHERE is_published = true;

-- Validation channels: sous-ensemble de la liste autorisée
ALTER TABLE public.listing ADD CONSTRAINT listing_channels_valid
  CHECK (channels <@ ARRAY['native','fb','lespac','kijiji']::text[]);

-- Auto-update updated_at
CREATE OR REPLACE FUNCTION public.touch_updated_at() RETURNS trigger LANGUAGE plpgsql AS $$
BEGIN NEW.updated_at := now(); RETURN NEW; END $$;

CREATE TRIGGER listing_touch BEFORE UPDATE ON public.listing
  FOR EACH ROW EXECUTE FUNCTION public.touch_updated_at();

-- Photos, attachées au VIN (pas au listing — WGI peut exister sans listing row)
CREATE TABLE public.vehicle_photo (
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid(),
  vin           text NOT NULL,
  storage_path  text NOT NULL UNIQUE,
  position      integer NOT NULL DEFAULT 0,
  is_hero       boolean NOT NULL DEFAULT false,
  uploaded_by   uuid REFERENCES auth.users(id) ON DELETE SET NULL,
  uploaded_at   timestamptz NOT NULL DEFAULT now()
);

CREATE INDEX vehicle_photo_vin_idx ON public.vehicle_photo(vin);
CREATE UNIQUE INDEX vehicle_photo_one_hero_per_vin
  ON public.vehicle_photo(vin) WHERE is_hero = true;

-- RLS
ALTER TABLE public.listing       ENABLE ROW LEVEL SECURITY;
ALTER TABLE public.vehicle_photo ENABLE ROW LEVEL SECURITY;

CREATE POLICY listing_auth_all ON public.listing
  FOR ALL TO authenticated USING (true) WITH CHECK (true);

CREATE POLICY vehicle_photo_auth_all ON public.vehicle_photo
  FOR ALL TO authenticated USING (true) WITH CHECK (true);

-- Storage bucket privé
INSERT INTO storage.buckets (id, name, public) VALUES ('vehicle-photos', 'vehicle-photos', false)
  ON CONFLICT (id) DO NOTHING;

CREATE POLICY vehicle_photos_read ON storage.objects
  FOR SELECT TO authenticated USING (bucket_id = 'vehicle-photos');
CREATE POLICY vehicle_photos_write ON storage.objects
  FOR INSERT TO authenticated WITH CHECK (bucket_id = 'vehicle-photos');
CREATE POLICY vehicle_photos_update ON storage.objects
  FOR UPDATE TO authenticated USING (bucket_id = 'vehicle-photos');
CREATE POLICY vehicle_photos_delete ON storage.objects
  FOR DELETE TO authenticated USING (bucket_id = 'vehicle-photos');
```

Appliquer via Claude: `mcp__plugin_supabase_supabase__apply_migration` avec `name='listings_photos'` et le contenu.

- [ ] **Step 2: Vérif RLS via MCP**

```sql
-- Comme anon → doit retourner 0 rows même en présence de listings
SELECT count(*) FROM public.listing;
```

Attendu comme service role (MCP): insère 1 fake listing puis re-requête — le test RLS réel se fait côté app.

- [ ] **Step 3: Commit le fichier migration**

```bash
git add supabase/
git commit -m "feat(db): listings + vehicle_photo tables + RLS + storage bucket"
```

---

## Task 1: Types TypeScript depuis Supabase

**Goal:** `lib/supabase/types.ts` avec types générés pour `Database`, utilisables par clients SSR.

**Verify:** `import { Database } from '@/lib/supabase/types'` compile.

- [ ] **Step 1:** `mcp__plugin_supabase_supabase__generate_typescript_types` → écrire dans `lib/supabase/types.ts`.

- [ ] **Step 2:** Mettre à jour `lib/supabase/client.ts` + `lib/supabase/server.ts` pour typer les clients:

```ts
import type { Database } from "@/lib/supabase/types";
// ...
createBrowserClient<Database>(...);
createServerClient<Database>(...);
```

- [ ] **Step 3:** `pnpm typecheck` — 0 erreur.

- [ ] **Step 4:** Commit.

---

## Task 2: Validateur "prêt à publier" + tests

**Goal:** Pure function qui détermine si un listing peut passer `is_published=true`.

**Verify:** `pnpm test` — 4 cas passés.

- [ ] **Step 1:** `lib/listings/publication.ts`:

```ts
export interface PublicationCandidate {
  price_cad: number;
  description_fr: string;
  photos: { is_hero: boolean }[];
}

export type PublicationError = "price_missing" | "description_missing" | "no_photos" | "no_hero";

export function validatePublication(l: PublicationCandidate): PublicationError | null {
  if (!(l.price_cad > 0)) return "price_missing";
  if (l.description_fr.trim().length === 0) return "description_missing";
  if (l.photos.length === 0) return "no_photos";
  if (!l.photos.some((p) => p.is_hero)) return "no_hero";
  return null;
}
```

- [ ] **Step 2:** `tests/unit/publication.test.ts` — 4 cas (manque prix, manque desc, 0 photo, pas de hero) + 1 cas valide.

- [ ] **Step 3:** Run `pnpm test` → 5 passed (nouveau fichier).

- [ ] **Step 4:** Commit.

---

## Task 3: Queries listings (list + byVin) avec merge WGI

**Goal:** Fonctions serveur qui mergent SERTI WGI + Supabase listing + count photos.

**Verify:** Unit test mock WGI/listing/photos → sortie correcte.

- [ ] **Step 1:** Étendre `lib/serti/wgi.ts`:

```ts
export async function listActiveVehicles(): Promise<Vehicle[]>; // WGISTA='A'
```

Query: `SELECT ... FROM SDSFC.WGI WHERE WGISTA='A' ORDER BY WGIUNM`. Réutiliser le mapper `s`/`n`. Ajouter aussi `cost: n(row.WGICST)` au type `Vehicle` (readonly côté client, jamais exposé au catalogue public).

- [ ] **Step 2:** `lib/listings/queries.ts`:

```ts
export interface InventoryRow extends Vehicle {
  price_cad: number;
  is_published: boolean;
  photo_count: number;
  has_hero: boolean;
}

export async function fetchInventory(): Promise<InventoryRow[]>;
export async function fetchVehicleByVin(vin: string): Promise<InventoryDetail | null>;
```

Pattern: fetch WGI actifs (SERTI) en parallèle avec SELECT sur listing + count photos par vin (Supabase). Left-join côté JS par VIN.

- [ ] **Step 3:** Tests unit mock WGI/Supabase → merge correct, VIN absent de listing → defaults (0, false).

- [ ] **Step 4:** Commit.

---

## Task 4: Server Actions (upsert listing, toggle publish)

**Goal:** Server Actions sauvegardent prix/desc/canaux/publié. Gate publication via `validatePublication`.

**Verify:** Test E2E léger (Vitest server-side) avec client supabase mocké.

- [ ] **Step 1:** `lib/listings/schema.ts`:

```ts
import { z } from "zod";

export const listingSchema = z.object({
  price_cad: z.number().nonnegative(),
  description_fr: z.string().trim().max(4000),
  channels: z.array(z.enum(["native","fb","lespac","kijiji"])).default(["native","fb","lespac","kijiji"]),
});
```

- [ ] **Step 2:** `lib/listings/actions.ts`:

```ts
"use server";
export async function upsertListing(vin: string, data: ListingInput): Promise<void>;
export async function togglePublished(vin: string, next: boolean): Promise<{ ok: true } | { ok: false; error: PublicationError }>;
```

`togglePublished` charge listing + photos du VIN, appelle `validatePublication`, retourne error ou commit.

- [ ] **Step 3:** Unit tests pour les 2 actions (mock Supabase client).

- [ ] **Step 4:** `pnpm add react-hook-form zod @hookform/resolvers`.

- [ ] **Step 5:** Commit.

---

## Task 5: Page liste `/inventaire`

**Goal:** Server Component liste `fetchInventory()`, tableau dense (style Option A), filtres make/category.

**Verify:** Navigateur → 66 lignes visibles, filtres fonctionnent (client-side), badge "Publié" sur lignes publiées.

- [ ] **Step 1:** `app/inventaire/page.tsx`:

```tsx
export default async function InventairePage() {
  const rows = await fetchInventory();
  return <InventaireTable rows={rows} />;
}
```

- [ ] **Step 2:** Client component `InventaireTable`:

Colonnes: Unit#, VIN (abrégé), Année, Make, Model, Km, Catégorie, Coûtant ($), Prix ($), Photos (3/15), Publié.

Filtres: input text (match VIN/unit/make/model), dropdown catégorie, toggle "Publié seulement".

Densité: rangée ~32px, font-mono sur VIN/chiffres (suit brainstorm memory Option A).

- [ ] **Step 3:** Étendre `middleware.ts`:

```ts
const needsAuth = request.nextUrl.pathname.startsWith("/dashboard") || request.nextUrl.pathname.startsWith("/inventaire");
```

- [ ] **Step 4:** Dashboard: ajouter `<Link href="/inventaire">Inventaire</Link>`.

- [ ] **Step 5:** Test manuel navigateur.

- [ ] **Step 6:** Commit.

---

## Task 6: Page edit `/inventaire/[vin]` (sans photos)

**Goal:** Edit prix/desc/canaux + toggle publié. Photo manager arrive Task 7.

**Verify:** Édite 1 véhicule, sauve, reload — valeurs persistent. Toggle publié sur listing incomplet → message erreur.

- [ ] **Step 1:** `app/inventaire/[vin]/page.tsx`:

```tsx
export default async function Page({ params }: { params: Promise<{ vin: string }> }) {
  const { vin } = await params;
  const detail = await fetchVehicleByVin(vin);
  if (!detail) notFound();
  return <EditLayout detail={detail} />;
}
```

- [ ] **Step 2:** `ListingForm` client component (react-hook-form + zod resolver). Champs: price_cad, description_fr (textarea), channels (checkboxes), is_published (toggle avec handler séparé → appelle `togglePublished`, affiche erreur retournée).

- [ ] **Step 3:** Panneau readonly WGI (make/model/year/km/color/**cost**). Cost affiché avec label "Coûtant (interne, ne pas divulguer)".

- [ ] **Step 4:** Tests manuels.

- [ ] **Step 5:** Commit.

---

## Task 7: Photo manager

**Goal:** Upload (multi-select), drag-to-reorder, marquer hero, supprimer. Max 15/VIN.

**Verify:** Upload 3 photos → apparaissent → drag change order → page reload → ordre persisté → delete 1 → count -1.

- [ ] **Step 1:** `pnpm add @dnd-kit/core @dnd-kit/sortable @dnd-kit/utilities`.

- [ ] **Step 2:** Server Actions dans `lib/listings/actions.ts`:

```ts
export async function uploadPhoto(vin: string, formData: FormData): Promise<{ id: string; url: string }>;
export async function deletePhoto(id: string): Promise<void>;
export async function reorderPhotos(vin: string, orderedIds: string[]): Promise<void>;
export async function setHero(vin: string, id: string): Promise<void>;
```

Storage path: `${vin}/${uuid}.${ext}`. Vérif serveur: content-type in `image/jpeg`/`image/png`/`image/webp`, taille max 10 MB. Resize serveur = NON (Plan 5 optimisation).

`setHero` unset les autres (transaction: UPDATE ... SET is_hero=false WHERE vin=$1; UPDATE ... SET is_hero=true WHERE id=$2).

- [ ] **Step 3:** Client component `PhotoManager` — grid responsive, drag-reorder via @dnd-kit, bouton upload, overlay "⭐ Hero" sur la cover, bouton X par photo. Sur 1ère photo uploadée quand 0 existe → auto `setHero`.

- [ ] **Step 4:** Signed URLs pour preview (Supabase `storage.from('vehicle-photos').createSignedUrl(path, 3600)`).

- [ ] **Step 5:** Gate: côté serveur, refus upload si count >= 15.

- [ ] **Step 6:** Tests manuels.

- [ ] **Step 7:** Commit.

---

## Task 8: CI + self-review

**Goal:** CI reste verte avec les nouvelles migrations.

**Verify:** Push → GitHub Actions verte.

- [ ] **Step 1:** CI ajoute `pnpm build` (jusqu'ici on ne build pas; les pages SSR doivent compiler).

- [ ] **Step 2:** `pnpm test && pnpm typecheck && pnpm lint && pnpm build` tous verts localement.

- [ ] **Step 3:** Push. Observer CI.

- [ ] **Step 4:** Self-review:
  - [ ] Tous les 6 success criteria verts
  - [ ] RLS testée (anon → 0 row en SELECT manuel via publishable key)
  - [ ] Cost jamais sérialisé vers client de catalogue public (pas encore de catalogue → ok Plan 2, vérifier Plan 3)
  - [ ] Aucun TODO/TBD dans le code livré
  - [ ] Noms cohérents: `fetchInventory`, `fetchVehicleByVin`, `upsertListing`, `togglePublished`

---

## Prochain plan (Plan 3 — catalogue public + feeds)

Présuppose Plan 2 vert. Plan 3:
- `/` catalogue public (style Option A, filtres, liste compacte)
- Route fiche véhicule `/vehicule/[vin]`
- Feed XML/CSV par canal: `/feed/kijiji.xml`, `/feed/lespac.xml`, `/feed/fb.xml`, `/feed/native.json`
- Tailscale Funnel pour exposer `/feed/*` + `/` publiquement (rest de l'app reste interne tailnet)
- Leads: bouton appeler + form contact (email vendeur + row Supabase)
